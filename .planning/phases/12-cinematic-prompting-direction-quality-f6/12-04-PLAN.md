---
phase: 12-cinematic-prompting-direction-quality-f6
plan: 04
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - backend/app/prompt_compile.py
autonomous: true
requirements: [DIRQ-02, DIRQ-06]

must_haves:
  truths:
    - "Cada prompt de segmento se ensambla por capas en orden fijo y verificable"
    - "La biblia (paleta + óptica + luz + grano/DOF) aparece en cada prompt"
    - "Hay bloque de negativos en Kling/Veo/genérico, AUSENTE en Runway"
    - "Hay aspect y duración en el prompt"
    - "La estructura/vocabulario difiere por proveedor (Kling sujeto-primero, Veo cámara-primero, Runway movimiento)"
    - "compile_prompt_markdown sigue funcionando (retrocompatible para callers actuales)"
  artifacts:
    - path: "backend/app/prompt_compile.py"
      provides: "compile_layered_prompt(subject_action, camera, bible, provider, aspect, duration_sec)"
      contains: "def compile_layered_prompt"
  key_links:
    - from: "backend/app/prompt_compile.py"
      to: "music_planner._materialize (plan 06)"
      via: "compile_layered_prompt invocado por segmento y proveedor"
      pattern: "compile_layered_prompt"
---

<objective>
Implementar DIRQ-02 (prompt por capas) y DIRQ-06 (tuning por proveedor): añadir
`compile_layered_prompt` a `prompt_compile.py` que ensambla, en orden fijo y verificable por substring,
`[sujeto+acción de la línea] + [cámara/movimiento] + [biblia] + [negativos] + [aspect/duración]`,
con estructura óptima por motor (Kling/Veo/Runway/genérico). Mantener `compile_prompt_markdown`
intacto para no romper los callers actuales.

Purpose: Hoy `compile_prompt_markdown` produce una frase fina sin biblia, sin negativos, sin aspect.
DIRQ-02 exige que la biblia esté en TODOS los prompts y que el orden sea testeable. DIRQ-06 exige que
Kling sea sujeto-primero (cámara al final), Veo cámara-primero, y Runway describa movimiento sin negativos.
Output: nueva función pura de strings, totalmente testeable por substring/índice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cinematic-prompting-direction-quality-f6/12-CONTEXT.md
@.planning/phases/12-cinematic-prompting-direction-quality-f6/12-PATTERNS.md
@backend/app/prompt_compile.py
@backend/tests/test_prompt_compile.py

<interfaces>
<!-- Firma actual a preservar (prompt_compile.py:6) -->
def compile_prompt_markdown(shots: list[dict[str, Any]], provider: str) -> str

<!-- Forma de la biblia (de plan 02 / visual_bible): claves a leer -->
bible = {subject, world, palette, optics, light_rule, grain_dof, style_attributes[], negatives[]}

<!-- Orden de capas objetivo (CONTEXT.md DIRQ-02), separador "; ":
     [sujeto+acción] ; [cámara/movimiento] ; [biblia: luz/óptica/paleta/grano] ; [negativos] ; [aspect, Ns]
     Tuning por proveedor (RESEARCH §Pattern 1):
       kling   → sujeto primero, "cámara:" AL FINAL antes de negativos; negativos como sufijo
       veo     → cámara/cinematografía PRIMERO; negativos por exclusión descriptiva ("Evitar: ...")
       runway  → describe MOVIMIENTO; SIN bloque "negativos:" (Runway los ignora/invierte)
       generic → sujeto; cámara; biblia; negativos; aspect -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: compile_layered_prompt con tuning por proveedor</name>
  <files>backend/app/prompt_compile.py</files>
  <read_first>
    - backend/app/prompt_compile.py (función fina actual y branch por proveedor :31-42)
    - 12-RESEARCH.md §Pattern 1 (compile_layered_prompt, branch por proveedor) + §Code Examples (asserts)
    - 12-RESEARCH.md §Anti-Patterns (Runway sin negativos; Kling endpoint de movimiento; no mezclar luces)
    - 12-PATTERNS.md §prompt_compile.py (orden de capas, retrocompatibilidad)
    - backend/tests/test_prompt_compile.py (contrato exacto a satisfacer)
  </read_first>
  <behavior>
    - compile_layered_prompt(subject_action, camera, bible, provider, aspect="16:9", duration_sec) → str.
    - La capa de biblia concatena luz (light_rule), óptica (optics), paleta (palette), grano/DOF (grain_dof).
    - kling: el texto empieza por subject_action; "cámara:" aparece DESPUÉS del sujeto; "negativos:" como sufijo
      con la lista de bible["negatives"]; termina con aspect + "{N}s". Verificable: index(subject) < index("cámara:").
    - veo: empieza por la capa de cámara/cinematografía; usa "Evitar: ..." (no la palabra cruda "no"); incluye aspect.
    - runway: empieza por subject_action; usa "movimiento de cámara: ..."; NO contiene la subcadena "negativos:";
      incluye aspect.
    - generic: orden sujeto; cámara; biblia; negativos; aspect.
    - En todos los proveedores con negativos, el bloque incluye cada elemento de bible["negatives"].
    - compile_prompt_markdown SIN cambios de firma; sigue pasando sus tests previos.
  </behavior>
  <action>
    En `backend/app/prompt_compile.py` añade `compile_layered_prompt(*, subject_action: str, camera: str,
    bible: dict[str, Any] | None, provider: str, aspect: str = "16:9", duration_sec: float | None = None) -> str`.
    Construye `bible_layer` desde las claves light_rule/optics/palette/grain_dof (con defaults vacíos seguros via
    `.get`), `negs = ", ".join(bible.get("negatives", []))`, `clock = f"{aspect}, {int(round(duration_sec or 0))}s"`.
    Implementa el if/elif/else por `provider` con el orden/forma del §Pattern 1: kling sujeto-primero + cámara al
    final + negativos sufijo; veo cámara-primero + "Evitar:"; runway movimiento + SIN negativos; generic completo.
    Usa separador "; " entre capas. Mantén la función PURA (sin I/O). NO toques `compile_prompt_markdown` salvo,
    si quieres, hacerlo delegar internamente (opcional) — pero su firma y salida actual deben seguir intactas para
    no romper music_planner._materialize ni los tests existentes.
  </action>
  <verify>
    <automated>cd backend; .venv/Scripts/python.exe -m pytest tests/test_prompt_compile.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Kling: el prompt contiene subject_action, substring de palette y de optics de la biblia, "negativos:" con cada negativo, "16:9" y "{N}s"; index(subject) < index("cámara:").
    - Runway: el prompt NO contiene la subcadena "negativos:".
    - Veo: el prompt empieza por la capa de cámara (la cámara aparece antes que subject_action).
    - `compile_prompt_markdown([shot], "kling")` sigue devolviendo lo de antes (tests previos verdes).
  </acceptance_criteria>
  <done>compile_layered_prompt implementado con tuning por proveedor; test_prompt_compile verde; compile_prompt_markdown intacto.</done>
</task>

</tasks>

<verification>
- `cd backend && .venv/Scripts/python.exe -m pytest tests/test_prompt_compile.py -x -q` verde (incluye `-k provider`).
- `cd backend && .venv/Scripts/python.exe -m pytest -q` verde (≥48 + nuevos); music_planner/render_client sin romper.
</verification>

<success_criteria>
DIRQ-02 + DIRQ-06 cumplidos: prompt por capas con orden verificable, biblia presente, negativos por
proveedor correctos (ausentes en Runway), aspect/duración presentes, y vocabulario óptimo por motor.
Listo para que el plan 06 lo invoque por segmento.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bible (puede venir de LLM) → prompt final | El prompt es export visible; no debe contener nombres de obra/artista |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Information disclosure / IP | nombre de obra/artista colado vía bible en el prompt | mitigate | La biblia llega ya saneada (plan 02 sanitize_visual_bible); el compilador no reintroduce nombres |
| T-12-07 | Tampering | negativos aplicados a Runway empeoran el resultado | mitigate | Branch runway OMITE la capa de negativos; test afirma ausencia de "negativos:" |
</threat_model>

<output>
Tras completar, crea `.planning/phases/12-cinematic-prompting-direction-quality-f6/12-04-SUMMARY.md`
</output>
