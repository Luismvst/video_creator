---
phase: 12-cinematic-prompting-direction-quality-f6
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_visual_bible.py
  - backend/tests/test_prompt_compile.py
  - backend/tests/test_camera_language.py
  - backend/tests/test_keyframes.py
  - backend/tests/test_onboarding_ai.py
  - backend/tests/test_render_client.py
  - backend/tests/test_music_planner.py
autonomous: true
requirements: [DIRQ-01, DIRQ-02, DIRQ-03, DIRQ-04, DIRQ-05, DIRQ-06, DIRQ-07]

must_haves:
  truths:
    - "Existen ficheros de test (stub) para cada DIRQ antes de implementar"
    - "Los stubs declaran el contrato esperado (firmas, claves, substrings) y fallan en rojo hasta que se implemente"
    - "La suite existente (≥48) sigue verde tras añadir los stubs marcados xfail/skip donde el módulo aún no existe"
  artifacts:
    - path: "backend/tests/test_visual_bible.py"
      provides: "Contrato DIRQ-01 (campos de la biblia, heurística sin key, sanitizador legal)"
    - path: "backend/tests/test_prompt_compile.py"
      provides: "Contrato DIRQ-02/06 (orden de capas + branch por proveedor)"
    - path: "backend/tests/test_camera_language.py"
      provides: "Contrato DIRQ-04 (mapa energía→cámara, neutral sin audio)"
    - path: "backend/tests/test_keyframes.py"
      provides: "Contrato DIRQ-05 (encadenado reference_image + image_url payload dry-run)"
    - path: "backend/tests/test_onboarding_ai.py"
      provides: "Contrato DIRQ-03 (riqueza 8-15 shots con intent/shot_size sin key)"
  key_links:
    - from: "backend/tests/test_render_client.py"
      to: "render_client.render_timeline"
      via: "import + reroll_indices"
      pattern: "reroll_indices"
    - from: "backend/tests/test_music_planner.py"
      to: "music_planner._materialize"
      via: "import + biblia en prompt_*"
      pattern: "visual_bible|camera_move"
---

<objective>
Crear los tests stub (Wave 0) que fijan el contrato de TODOS los DIRQ ANTES de implementar.
Esto cierra los huecos listados en 12-VALIDATION.md §Wave 0 y garantiza la regla Nyquist: cada
tarea posterior tendrá un `<automated>` que ejecuta un test ya escrito.

Purpose: Sin estos stubs, las tareas de implementación no tienen criterio de aceptación
ejecutable. Escribir el test primero clava la forma exacta (firmas, claves, substrings, orden de
capas) que el resto de la fase debe satisfacer.
Output: 5 ficheros de test nuevos + 2 extendidos, importando los módulos/funciones objetivo.
Los tests de módulos aún inexistentes se marcan con `pytest.importorskip` o `pytest.mark.xfail`
para que la suite global siga verde hasta que cada plan los implemente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cinematic-prompting-direction-quality-f6/12-VALIDATION.md
@.planning/phases/12-cinematic-prompting-direction-quality-f6/12-RESEARCH.md
@backend/tests/test_render_client.py
@backend/tests/test_music_planner.py
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Stubs de módulos nuevos — visual_bible, prompt_compile, camera_language</name>
  <files>backend/tests/test_visual_bible.py, backend/tests/test_prompt_compile.py, backend/tests/test_camera_language.py</files>
  <read_first>
    - backend/tests/test_render_client.py (estilo: docstring "Puros + dry-run", fábricas locales `_segs`)
    - backend/tests/test_music_planner.py (estilo: fixtures SHOTS/_timings, asserts puros)
    - 12-RESEARCH.md §Code Examples (asserts objetivo de substring/orden ya escritos)
    - 12-RESEARCH.md §Validation Architecture (mapa Req→Test→comando)
  </read_first>
  <action>
    Crea `backend/tests/test_visual_bible.py`, `backend/tests/test_prompt_compile.py`,
    `backend/tests/test_camera_language.py`. Importa con `pytest.importorskip("app.visual_bible")`
    /`app.camera_language` al inicio (prompt_compile ya existe, no requiere importorskip pero
    `compile_layered_prompt` sí; envuelve esas funciones con `pytest.importorskip` o un try/except a
    `pytest.skip`).
    test_visual_bible: afirma que `build_visual_bible(brief, lyrics_text, title=..., mood=..., prefer_llm=False)`
    devuelve una tupla `(dict, str)` cuyo dict tiene las 8 claves
    `subject/world/palette/optics/light_rule/grain_dof/style_attributes/negatives`; que `style_attributes`
    y `negatives` son listas; y un test de sanitizador `sanitize_visual_bible({"style_attributes":["estilo de Tarantino"]})`
    elimina la entrada con "estilo de" (substring prohibido).
    test_prompt_compile: replica los dos tests del research (`test_layered_prompt_kling_order_and_bible_present`
    y `test_layered_prompt_runway_has_no_negatives`) contra `compile_layered_prompt(subject_action=, camera=,
    bible=, provider=, aspect=, duration_sec=)`. Afirma orden Kling (índice de "mujer" < índice de "cámara:"),
    biblia presente (substring de paleta y óptica), bloque "negativos:" presente en kling y AUSENTE en runway,
    y "16:9"/"4s" presentes.
    test_camera_language: afirma `camera_for_energy("high"|"mid"|"low"|"neutral")` devuelve dict con
    `shot_size` y `camera_move`; que high difiere de low; y que un nivel desconocido cae a "neutral".
    Sigue el estilo: docstring de cabecera declarando "Puros: sin red ni binarios".
  </action>
  <verify>
    <automated>cd backend; .venv/Scripts/python.exe -m pytest tests/test_visual_bible.py tests/test_prompt_compile.py tests/test_camera_language.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Los 3 ficheros existen e importan sus módulos objetivo vía importorskip/skip.
    - Al ejecutar, los tests se SALTAN (skipped) porque el módulo/función aún no existe — NO fallan en error de colección.
    - La suite completa `python -m pytest -q` sigue verde (≥48 passed, + N skipped), sin errores de import.
  </acceptance_criteria>
  <done>3 ficheros de test creados; suite global sin rojos (skips permitidos).</done>
</task>

<task type="auto">
  <name>Tarea 2: Stubs de DIRQ-03/05/07 — onboarding_ai, keyframes; extender render_client y music_planner</name>
  <files>backend/tests/test_onboarding_ai.py, backend/tests/test_keyframes.py, backend/tests/test_render_client.py, backend/tests/test_music_planner.py</files>
  <read_first>
    - backend/tests/test_render_client.py (extender: `_segs`, patrón de asserts dry-run)
    - backend/tests/test_music_planner.py (extender: SHOTS, plan_timeline/_materialize)
    - backend/app/onboarding_ai.py:73-140 (heuristic_onboarding_package: hoy 2 shots)
    - backend/app/keyframes.py:53-93 (plan_keyframes chain)
    - 12-RESEARCH.md §Code Examples (test_keyframe_chain_references_previous, test_reroll_only_marked_indices)
  </read_first>
  <action>
    Crea `backend/tests/test_onboarding_ai.py` y `backend/tests/test_keyframes.py`. Extiende
    `backend/tests/test_render_client.py` y `backend/tests/test_music_planner.py` (añade funciones de test,
    NO modifiques los tests existentes).
    test_onboarding_ai: función `test_shots_richness_heuristic` — `heuristic_onboarding_package(lyrics, brief,
    title=None, mood=None)` produce `shots_json` con un array de entre 8 y 15 shots, y cada shot tiene las claves
    nuevas opcionales `intent` y `shot_size` además de `slug/camera/action/notes`. Usa una letra de varias estrofas
    (líneas en blanco) como fixture. Marca con xfail/skip suave si aún devuelve 2 (estado actual) — el assert debe
    ser el contrato final (8-15).
    test_keyframes: replica `test_keyframe_chain_references_previous` — `plan_keyframes(segs, chain=True, dry_run=True)`
    da `keyframes[0]["reference_image"] is None`, `keyframes[1]["reference_image"]` termina en "kf_001.png",
    `keyframes[2]` en "kf_002.png" (esto YA pasa hoy → test verde inmediato, fija el contrato DIRQ-05 plan).
    Añade `test_keyframe_real_payload_includes_image_url` marcado xfail: cuando exista el camino real, el payload
    a fal del segmento i incluye `image_url` = reference del i-1 (verificable en dry-run vía un parámetro de inspección
    o un helper puro que construya el payload).
    Extiende test_render_client: `test_reroll_only_marked_indices` — `render_timeline(_segs(10), provider="kling",
    dry_run=True, reroll_indices=[2,5])` → los `index` renderizados son exactamente [2,5] y la estimación se calcula
    sobre 2 segmentos. Márcalo skip-if-not-supported hasta que el parámetro exista.
    Extiende test_music_planner: `test_bible_injected_in_every_prompt` — tras `plan_timeline(SHOTS,...)` con una
    `bible` pasada (vía el mecanismo que defina el plan 06), cada `prompt_generic` no vacío contiene el substring de
    la paleta de la biblia; y `test_camera_modulated_by_energy` — alta energía produce `shot.camera_move`/`shot.shot_size`
    más intensos que baja. Márcalos skip/xfail hasta implementación.
  </action>
  <verify>
    <automated>cd backend; .venv/Scripts/python.exe -m pytest tests/test_onboarding_ai.py tests/test_keyframes.py tests/test_render_client.py tests/test_music_planner.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test_keyframes::test_keyframe_chain_references_previous PASA hoy (el plan dry-run ya encadena).
    - Los stubs de funcionalidad no implementada (reroll, biblia inyectada, cámara-energía, shots 8-15, image_url) están skipped o xfail — NO rojos.
    - `cd backend && .venv/Scripts/python.exe -m pytest -q` sigue verde (≥48 passed + skips/xfails).
  </acceptance_criteria>
  <done>2 ficheros nuevos + 2 extendidos; suite global sin rojos; contrato DIRQ-05 (plan) ya verde.</done>
</task>

</tasks>

<verification>
- `cd backend && .venv/Scripts/python.exe -m pytest -q` → 0 fallos (skips/xfails permitidos para lo aún no implementado).
- Cada uno de los 7 DIRQ tiene al menos un test (stub o real) que lo referencia por nombre de función/clave.
- Ningún test existente fue modificado en su lógica (solo se añadieron funciones nuevas).
</verification>

<success_criteria>
Los 7 ítems de 12-VALIDATION.md §Wave 0 quedan cubiertos por ficheros de test reales; la suite global
permanece verde; cada plan posterior puede usar `pytest tests/test_<módulo> -x` como su `<automated>`.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguno nuevo) | Los stubs son código de test local sin red ni I/O externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | Tests que llamarían a red/FAL/OpenAI | mitigate | Prohibido llamar a red en tests; solo heurístico + dry-run (estilo test_render_client) |
</threat_model>

<output>
Tras completar, crea `.planning/phases/12-cinematic-prompting-direction-quality-f6/12-01-SUMMARY.md`
</output>
