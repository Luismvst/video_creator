---
phase: 12-cinematic-prompting-direction-quality-f6
plan: 06
type: execute
wave: 3
depends_on: [12-01, 12-03, 12-04, 12-05]
files_modified:
  - backend/app/music_planner.py
  - backend/app/pipeline.py
autonomous: true
requirements: [DIRQ-02, DIRQ-04]

must_haves:
  truths:
    - "Cada segmento compila prompt_generic/runway/kling con compile_layered_prompt (por capas)"
    - "La biblia visual aparece en TODOS los prompts no vacíos de los segmentos"
    - "El shot de cada segmento lleva camera_move/shot_size modulados por la energía de su sección"
    - "Sin audio (solo-letra) la modulación cae a perfil neutro y la suite no se rompe"
    - "La forma de segmento canónica (campos F3/F5) permanece intacta; lo nuevo es aditivo"
  artifacts:
    - path: "backend/app/music_planner.py"
      provides: "_materialize inyecta biblia + cámara-por-energía y usa compile_layered_prompt"
      contains: "compile_layered_prompt"
  key_links:
    - from: "backend/app/music_planner.py"
      to: "prompt_compile.compile_layered_prompt"
      via: "construcción de prompt_* por segmento"
      pattern: "compile_layered_prompt"
    - from: "backend/app/music_planner.py"
      to: "camera_language.camera_for_energy"
      via: "modulación de cámara por energía de sección"
      pattern: "camera_for_energy"
    - from: "backend/app/pipeline.py"
      to: "plan_timeline (bible)"
      via: "pasar visual_bible del paquete al planner"
      pattern: "visual_bible"
---

<objective>
Cablear DIRQ-02 (biblia en cada prompt) y DIRQ-04 (cámara modulada por energía) dentro del planner.
`music_planner._materialize` deja de usar la frase fina `compile_prompt_markdown` y pasa a usar
`compile_layered_prompt` (plan 04), inyectando la biblia (plan 02/05) y el vocabulario de cámara
(plan 03) derivado de la energía de la sección de cada segmento. `pipeline.py` pasa la biblia del
paquete al planner.

Purpose: Este es el punto de unión donde toda la fase se materializa en el texto que entra al motor.
La biblia debe estar en TODOS los prompts (continuidad), y la cámara debe escalar con la energía
(estribillo dinámico, verso sostenido), degradando a neutro sin audio.
Output: `_materialize` enriquecido + `plan_timeline`/`plan_segments_from_timings` aceptando `bible`,
y `pipeline.py` pasándola.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cinematic-prompting-direction-quality-f6/12-CONTEXT.md
@.planning/phases/12-cinematic-prompting-direction-quality-f6/12-PATTERNS.md
@backend/app/music_planner.py
@backend/app/prompt_compile.py
@backend/app/camera_language.py
@backend/app/pipeline.py
@backend/tests/test_music_planner.py

<interfaces>
<!-- music_planner._materialize:108-139 — punto de inyección por segmento -->
<!-- _is_high_energy(t, sections, threshold) / _section_energy_threshold(sections) ya existen :56-70 -->
<!-- compile_layered_prompt(*, subject_action, camera, bible, provider, aspect, duration_sec) -> str  (plan 04) -->
<!-- camera_for_energy(level) -> {shot_size, camera_move}; energy_level_for(mean_energy, threshold) -> str (plan 03) -->
<!-- pipeline.py:67-80 construye pkg y llama plan_timeline; pkg trae "visual_bible_json" (plan 05) -->

<!-- Forma de segmento canónica a NO romper (F3/F5):
     index/start_sec/end_sec/duration_sec/shot/prompt_generic/prompt_runway/prompt_kling/source.
     Nuevos campos aditivos: shot["shot_size"], shot["camera_move"], shot["intent"] (de los shots ricos). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: _materialize usa compile_layered_prompt + biblia + cámara-por-energía</name>
  <files>backend/app/music_planner.py</files>
  <read_first>
    - backend/app/music_planner.py:108-139 (_materialize, construcción de prompts) y :56-70 (energía)
    - backend/app/prompt_compile.py (compile_layered_prompt, plan 04)
    - backend/app/camera_language.py (camera_for_energy / energy_level_for, plan 03)
    - 12-PATTERNS.md §music_planner.py (punto de inyección, degradación neutral, forma intacta)
    - 12-RESEARCH.md §Pitfall 3 (biblia ausente si shot vacío) y §Pitfall 5 (sin audio → neutral)
    - backend/tests/test_music_planner.py (tests existentes + stubs test_bible_injected/test_camera_modulated)
  </read_first>
  <behavior>
    - _materialize acepta un parámetro `bible: dict | None` y `sections`/`threshold` para clasificar energía.
    - Por cada segmento: clasifica el nivel de energía del intervalo [s,e] (energy_level_for usando
      _section_energy_threshold(sections); "neutral" si no hay secciones), obtiene camera_for_energy(level),
      y enriquece `shot` con shot_size y camera_move (aditivo; no pisa intent si ya viene del shot rico).
    - Construye subject_action desde shot["action"] (+ las líneas de letra del segmento si ayuda) y camera desde
      shot["camera_move"] (o shot["camera"]). Llama compile_layered_prompt(subject_action=, camera=, bible=bible,
      provider="generic"|"runway"|"kling", aspect="16:9", duration_sec=duración del segmento) para cada prompt_*.
    - La biblia aparece en cada prompt no vacío (substring de palette). Sin shot, los prompts pueden ir vacíos
      (comportamiento previo), pero con DIRQ-03 siempre hay shots.
    - Sin audio (sections None): nivel "neutral"; la suite solo-letra (test_plan_timeline_falls_back_to_heuristic) sigue verde.
    - Campos canónicos del segmento intactos.
  </behavior>
  <action>
    Modifica `_materialize` para aceptar `bible: Optional[dict[str, Any]] = None`,
    `sections: Optional[list[dict]] = None`. Calcula `threshold = _section_energy_threshold(sections)` una vez.
    Dentro del bucle, antes de compilar: `level = energy_level_for(<mean_energy de la sección de s>, threshold)`
    — para obtener la mean_energy reutiliza la lógica de `_is_high_energy`/recorrido de secciones (extrae un helper
    puro `_section_energy_at(t, sections)` si ayuda). `cam = camera_for_energy(level)`; mete `shot["shot_size"]` y
    `shot["camera_move"]` (sin borrar `shot["intent"]`/`shot["camera"]`). Sustituye las 3 líneas
    `compile_prompt_markdown([shot], ...)` por `compile_layered_prompt(subject_action=shot.get("action",""),
    camera=shot.get("camera_move") or shot.get("camera",""), bible=bible, provider=..., duration_sec=round(e-s,2))`.
    Propaga `bible`/`sections` desde `plan_segments_from_timings` (ya recibe sections) y desde `_enforce_max_clip`/
    la rama heurística (donde sections es None → neutral). Importa `from .camera_language import camera_for_energy,
    energy_level_for`. Mantén `from .prompt_compile import compile_layered_prompt` (puedes dejar también el import
    viejo si algún punto lo usa). No rompas firmas públicas: añade `bible` como kwarg opcional a
    `plan_segments_from_timings` y a `plan_timeline`.
  </action>
  <verify>
    <automated>cd backend; .venv/Scripts/python.exe -m pytest tests/test_music_planner.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_bible_injected_in_every_prompt: con una bible dada, cada prompt_generic no vacío contiene el substring de la paleta.
    - test_camera_modulated_by_energy: en sección de alta energía, shot["camera_move"]/shot["shot_size"] difieren de los de baja energía.
    - Sin audio: todos los segmentos llevan perfil neutral; los tests existentes de music_planner siguen verdes.
    - Cada segmento conserva index/start_sec/end_sec/duration_sec/shot/prompt_generic/prompt_runway/prompt_kling/source.
  </acceptance_criteria>
  <done>_materialize por capas + cámara-energía; biblia en cada prompt; forma de segmento intacta; tests verdes.</done>
</task>

<task type="auto">
  <name>Tarea 2: pipeline.py pasa la biblia del paquete al planner</name>
  <files>backend/app/pipeline.py</files>
  <read_first>
    - backend/app/pipeline.py:66-91 (build_onboarding_package → shots → plan_timeline → render)
    - backend/app/music_planner.py (plan_timeline con nuevo kwarg bible)
    - backend/tests/test_smoke.py (ejercita el flujo vía API; no romper)
  </read_first>
  <action>
    En `pipeline.run_pipeline`, tras `pkg, shots_note = build_onboarding_package(...)`, parsea la biblia:
    `bible = json.loads(pkg.get("visual_bible_json") or "{}")` con try/except (fallback `{}`). Pásala a
    `plan_timeline(shots, line_timings=..., music=..., total_seconds=..., title=..., bible=bible)`.
    No cambies el resto del flujo (F3/F4/F5). Asegura que el manifiesto devuelto siga siendo JSON-serializable.
  </action>
  <verify>
    <automated>cd backend; .venv/Scripts/python.exe -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `run_pipeline(..., dry_run=True)` corre sin red y los segmentos del manifiesto traen la biblia en prompt_generic (substring de paleta).
    - test_smoke y el resto de la suite verdes (≥48 + nuevos).
  </acceptance_criteria>
  <done>pipeline pasa la biblia al planner; suite completa verde; dry-run end-to-end con biblia en los prompts.</done>
</task>

</tasks>

<verification>
- `cd backend && .venv/Scripts/python.exe -m pytest tests/test_music_planner.py -x -q` verde.
- `cd backend && .venv/Scripts/python.exe -m pytest -q` verde (≥48 + nuevos); test_smoke incluido.
- Inspección manual opcional (artefacto CONTEXT.md): correr run_pipeline dry-run y comparar un prompt antes/después.
</verification>

<success_criteria>
DIRQ-02 (biblia en cada prompt, por capas) y DIRQ-04 (cámara modulada por energía con degradación neutral)
cableados end-to-end en dry-run, sin romper la forma de segmento ni la suite.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paquete/biblia → prompts de segmento (export) | el prompt es visible; biblia ya saneada aguas arriba |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Tampering | romper la forma de segmento (KeyError en F3/F5) | mitigate | solo añadir claves opcionales; tests test_render_client/test_music_planner existentes deben seguir verdes sin tocarlos |
| T-12-11 | DoS | visual_bible_json malformado en el paquete | mitigate | json.loads con try/except → fallback {} en pipeline |
</threat_model>

<output>
Tras completar, crea `.planning/phases/12-cinematic-prompting-direction-quality-f6/12-06-SUMMARY.md`
</output>
