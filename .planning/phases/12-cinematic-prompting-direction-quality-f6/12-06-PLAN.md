---
phase: 12-cinematic-prompting-direction-quality-f6
plan: 06
type: execute
wave: 3
depends_on: [12-01, 12-03, 12-04, 12-05]
files_modified:
  - backend/app/music_planner.py
  - backend/app/pipeline.py
autonomous: true
requirements: [DIRQ-02, DIRQ-04, DIRQ-06]

must_haves:
  truths:
    - "Cada segmento compila prompt_generic/runway/kling/veo con compile_layered_prompt (por capas)"
    - "prompt_veo está presente en cada segmento (aditivo) y contiene la biblia + aspect"
    - "La biblia visual aparece en TODOS los prompts no vacíos de los segmentos"
    - "El shot de cada segmento lleva camera_move/shot_size modulados por la energía de su sección vía _section_energy_at"
    - "Sin audio (solo-letra) la modulación cae a perfil neutro y la suite no se rompe"
    - "La forma de segmento canónica (campos F3/F5) permanece intacta; lo nuevo (prompt_veo, shot_size, camera_move) es aditivo"
  artifacts:
    - path: "backend/app/music_planner.py"
      provides: "_materialize inyecta biblia + cámara-por-energía (vía _section_energy_at) y usa compile_layered_prompt para generic/runway/kling/veo"
      contains: "compile_layered_prompt"
  key_links:
    - from: "backend/app/music_planner.py"
      to: "prompt_compile.compile_layered_prompt"
      via: "construcción de prompt_* por segmento (incl. prompt_veo)"
      pattern: "compile_layered_prompt"
    - from: "backend/app/music_planner.py"
      to: "camera_language.camera_for_energy"
      via: "modulación de cámara por energía de sección (_section_energy_at → energy_level_for)"
      pattern: "camera_for_energy|_section_energy_at"
    - from: "backend/app/pipeline.py"
      to: "plan_timeline (bible)"
      via: "pasar visual_bible del paquete al planner"
      pattern: "visual_bible"
---

<objective>
Cablear DIRQ-02 (biblia en cada prompt), DIRQ-04 (cámara modulada por energía) y DIRQ-06 (prompt_veo, el
cuarto campo de prompt) dentro del planner. `music_planner._materialize` deja de usar la frase fina
`compile_prompt_markdown` y pasa a usar `compile_layered_prompt` (plan 04), inyectando la biblia (plan 02/05)
y el vocabulario de cámara (plan 03) derivado de la energía de la sección de cada segmento. Rellena los
CUATRO prompts por segmento: `prompt_generic`, `prompt_runway`, `prompt_kling` y `prompt_veo` (aditivo, Q3
RESUELTO, H2). `pipeline.py` pasa la biblia del paquete al planner.

Purpose: Este es el punto de unión donde toda la fase se materializa en el texto que entra al motor.
La biblia debe estar en TODOS los prompts (continuidad), la cámara debe escalar con la energía
(estribillo dinámico, verso sostenido) degradando a neutro sin audio, y Veo debe quedar cableado de
extremo a extremo (su texto cámara-primero vive en `prompt_veo`, que el plan 07 enruta).
Output: `_materialize` enriquecido + `plan_timeline`/`plan_segments_from_timings` aceptando `bible`,
y `pipeline.py` pasándola. Cada segmento gana `prompt_veo` sin perder ningún campo canónico.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cinematic-prompting-direction-quality-f6/12-CONTEXT.md
@.planning/phases/12-cinematic-prompting-direction-quality-f6/12-PATTERNS.md
@backend/app/music_planner.py
@backend/app/prompt_compile.py
@backend/app/camera_language.py
@backend/app/pipeline.py
@backend/tests/test_music_planner.py

<interfaces>
<!-- music_planner._materialize:108-139 — punto de inyección por segmento -->
<!-- _section_energy_threshold(sections):56-60 e _is_high_energy(t, sections, threshold):63-70 ya existen -->
<!-- M3 — firma fija del helper de energía→cámara que el test_camera_modulated_by_energy asume:
       def _section_energy_at(t: float, sections: Optional[list[dict]]) -> Optional[float]
       devuelve la mean_energy de la sección cuyo [start_sec,end_sec) contiene t, o None si no hay sección.
     El nivel se obtiene con energy_level_for(_section_energy_at(t, sections), threshold). -->
<!-- compile_layered_prompt(*, subject_action, camera, bible, provider, aspect, duration_sec) -> str  (plan 04; provider ∈ {generic,runway,kling,veo}) -->
<!-- camera_for_energy(level) -> {shot_size, camera_move}; energy_level_for(mean_energy, threshold) -> str (plan 03) -->
<!-- pipeline.py:67-80 construye pkg y llama plan_timeline; pkg trae "visual_bible_json" (plan 05) -->

<!-- Forma de segmento canónica a NO romper (F3/F5):
     index/start_sec/end_sec/duration_sec/shot/prompt_generic/prompt_runway/prompt_kling/source.
     Nuevos campos aditivos: prompt_veo (nivel segmento), shot["shot_size"], shot["camera_move"], shot["intent"]. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: _materialize usa compile_layered_prompt (generic/runway/kling/veo) + biblia + cámara-por-energía (_section_energy_at)</name>
  <files>backend/app/music_planner.py</files>
  <read_first>
    - backend/app/music_planner.py:108-139 (_materialize, construcción de prompts) y :56-70 (energía: _section_energy_threshold, _is_high_energy)
    - backend/app/prompt_compile.py (compile_layered_prompt, plan 04; branch veo)
    - backend/app/camera_language.py (camera_for_energy / energy_level_for, plan 03)
    - 12-PATTERNS.md §music_planner.py (punto de inyección, degradación neutral, forma intacta)
    - 12-RESEARCH.md §Pitfall 3 (biblia ausente si shot vacío) y §Pitfall 5 (sin audio → neutral)
    - 12-RESEARCH.md §Open Questions Q3 (RESUELTO: prompt_veo aditivo por segmento)
    - backend/tests/test_music_planner.py (tests existentes + stubs test_bible_injected/test_camera_modulated)
  </read_first>
  <behavior>
    - _materialize acepta un parámetro `bible: dict | None` y `sections`/`threshold` para clasificar energía.
    - M3 (firma fija): existe un helper puro `_section_energy_at(t, sections)` que devuelve la mean_energy de la
      sección cuyo [start_sec,end_sec) contiene t (o None). _materialize lo usa para clasificar la energía del
      intervalo de cada segmento: `level = energy_level_for(_section_energy_at(s, sections), threshold)` ("neutral"
      si no hay secciones), `cam = camera_for_energy(level)`, y enriquece `shot` con shot_size y camera_move
      (aditivo; no pisa intent si ya viene del shot rico). Alta energía → camera_move/shot_size MÁS dinámicos que
      baja (mismo dict que camera_for_energy("high") vs ("low")).
    - Construye subject_action desde shot["action"] (+ las líneas de letra del segmento si ayuda) y camera desde
      shot["camera_move"] (o shot["camera"]). Llama compile_layered_prompt(subject_action=, camera=, bible=bible,
      provider=..., aspect="16:9", duration_sec=duración del segmento) para CADA uno de los cuatro prompts:
      provider="generic" → prompt_generic, "runway" → prompt_runway, "kling" → prompt_kling, "veo" → prompt_veo.
    - prompt_veo es ADITIVO: se añade junto a los tres existentes; ningún campo canónico se elimina.
    - La biblia aparece en cada prompt no vacío (substring de palette), incluido prompt_veo. Sin shot, los prompts
      pueden ir vacíos (comportamiento previo), pero con DIRQ-03 siempre hay shots.
    - Sin audio (sections None): nivel "neutral"; la suite solo-letra (test_plan_timeline_falls_back_to_heuristic) sigue verde.
    - Campos canónicos del segmento intactos.
  </behavior>
  <action>
    Modifica `_materialize` para aceptar `bible: Optional[dict[str, Any]] = None`,
    `sections: Optional[list[dict]] = None`. Calcula `threshold = _section_energy_threshold(sections)` una vez.
    Añade el helper puro `_section_energy_at(t, sections)` (recorre sections; devuelve mean_energy de la sección
    que contiene t o None) — esta es la firma que el test_camera_modulated_by_energy del plan 01 asume (M3).
    Dentro del bucle, antes de compilar: `level = energy_level_for(_section_energy_at(s, sections), threshold)`;
    `cam = camera_for_energy(level)`; mete `shot["shot_size"]` y `shot["camera_move"]` (sin borrar
    `shot["intent"]`/`shot["camera"]`). Sustituye las 3 líneas `compile_prompt_markdown([shot], ...)` por CUATRO
    llamadas a `compile_layered_prompt(subject_action=shot.get("action",""), camera=shot.get("camera_move") or
    shot.get("camera",""), bible=bible, provider=<generic|runway|kling|veo>, duration_sec=round(e-s,2))`,
    asignando la cuarta a `seg["prompt_veo"]` (campo aditivo nuevo). Propaga `bible`/`sections` desde
    `plan_segments_from_timings` (ya recibe sections) y desde `_enforce_max_clip`/la rama heurística (donde
    sections es None → neutral). Importa `from .camera_language import camera_for_energy, energy_level_for`.
    Mantén `from .prompt_compile import compile_layered_prompt` (puedes dejar también el import viejo si algún
    punto lo usa). No rompas firmas públicas: añade `bible` como kwarg opcional a `plan_segments_from_timings` y a
    `plan_timeline`.
  </action>
  <verify>
    <automated>cd backend; .venv/Scripts/python.exe -m pytest tests/test_music_planner.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Cada segmento tiene `prompt_veo` (campo aditivo) además de prompt_generic/prompt_runway/prompt_kling; con shot no vacío, prompt_veo contiene el substring de la paleta de la biblia y el aspect ("16:9").
    - test_bible_injected_in_every_prompt: con una bible dada, cada prompt_generic no vacío contiene el substring de la paleta.
    - test_camera_modulated_by_energy: con sections de mean_energy dispares, shot["camera_move"]/shot["shot_size"] de los segmentos en la sección de ALTA energía difieren (más dinámicos) de los de la sección de BAJA energía; la clasificación pasa por `_section_energy_at`.
    - Sin audio: todos los segmentos llevan perfil neutral; los tests existentes de music_planner siguen verdes.
    - Cada segmento conserva index/start_sec/end_sec/duration_sec/shot/prompt_generic/prompt_runway/prompt_kling/source (ningún campo eliminado).
  </acceptance_criteria>
  <done>_materialize por capas (4 prompts, incl. prompt_veo) + cámara-energía vía _section_energy_at; biblia en cada prompt; forma de segmento intacta; tests verdes.</done>
</task>

<task type="auto">
  <name>Tarea 2: pipeline.py pasa la biblia del paquete al planner</name>
  <files>backend/app/pipeline.py</files>
  <read_first>
    - backend/app/pipeline.py:66-91 (build_onboarding_package → shots → plan_timeline → render)
    - backend/app/music_planner.py (plan_timeline con nuevo kwarg bible)
    - backend/tests/test_smoke.py (ejercita el flujo vía API; no romper)
  </read_first>
  <action>
    En `pipeline.run_pipeline`, tras `pkg, shots_note = build_onboarding_package(...)`, parsea la biblia:
    `bible = json.loads(pkg.get("visual_bible_json") or "{}")` con try/except (fallback `{}`). Pásala a
    `plan_timeline(shots, line_timings=..., music=..., total_seconds=..., title=..., bible=bible)`.
    No cambies el resto del flujo (F3/F4/F5). Asegura que el manifiesto devuelto siga siendo JSON-serializable
    (incluido el nuevo campo prompt_veo, que es un string).
  </action>
  <verify>
    <automated>cd backend; .venv/Scripts/python.exe -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `run_pipeline(..., dry_run=True)` corre sin red y los segmentos del manifiesto traen la biblia en prompt_generic y en prompt_veo (substring de paleta).
    - test_smoke y el resto de la suite verdes (≥48 + nuevos).
  </acceptance_criteria>
  <done>pipeline pasa la biblia al planner; suite completa verde; dry-run end-to-end con biblia en los prompts (incl. prompt_veo).</done>
</task>

</tasks>

<verification>
- `cd backend && .venv/Scripts/python.exe -m pytest tests/test_music_planner.py -x -q` verde.
- `cd backend && .venv/Scripts/python.exe -m pytest -q` verde (≥48 + nuevos); test_smoke incluido.
- Inspección manual opcional (artefacto CONTEXT.md): correr run_pipeline dry-run y comparar un prompt antes/después (incl. prompt_veo).
</verification>

<success_criteria>
DIRQ-02 (biblia en cada prompt, por capas), DIRQ-04 (cámara modulada por energía vía _section_energy_at con
degradación neutral) y DIRQ-06 (prompt_veo aditivo por segmento) cableados end-to-end en dry-run, sin romper
la forma de segmento ni la suite.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paquete/biblia → prompts de segmento (export) | el prompt es visible; biblia ya saneada aguas arriba |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Tampering | romper la forma de segmento (KeyError en F3/F5) | mitigate | solo añadir claves opcionales (prompt_veo, shot_size, camera_move); tests test_render_client/test_music_planner existentes deben seguir verdes sin tocarlos |
| T-12-11 | DoS | visual_bible_json malformado en el paquete | mitigate | json.loads con try/except → fallback {} en pipeline |
</threat_model>

<output>
Tras completar, crea `.planning/phases/12-cinematic-prompting-direction-quality-f6/12-06-SUMMARY.md`
</output>
</content>
