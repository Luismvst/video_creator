---
phase: 12-cinematic-prompting-direction-quality-f6
plan: 05
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - backend/app/onboarding_ai.py
autonomous: true
requirements: [DIRQ-03, DIRQ-01]

must_haves:
  truths:
    - "La ruta heurística (sin key) genera entre 8 y 15 shots con intención narrativa, repartidos por secciones de letra"
    - "Cada shot conserva slug/camera/action/notes y añade intent y shot_size opcionales"
    - "El paquete de onboarding incluye la biblia visual (visual_bible_json) generada/saneada"
    - "La ruta LLM también pide 8-15 shots ricos sin romper la forma del paquete"
  artifacts:
    - path: "backend/app/onboarding_ai.py"
      provides: "heuristic_onboarding_package con 8-15 shots ricos + visual_bible_json en el paquete"
      contains: "visual_bible_json"
  key_links:
    - from: "backend/app/onboarding_ai.py"
      to: "backend/app/visual_bible.py"
      via: "build_visual_bible fusiona la biblia en el paquete"
      pattern: "build_visual_bible|visual_bible_json"
    - from: "backend/app/onboarding_ai.py"
      to: "suggest_section_spans"
      via: "reparto de shots por sección de letra (sin key)"
      pattern: "suggest_section_spans"
---

<objective>
Implementar DIRQ-03 (shots ricos por defecto): la ruta heurística sin key debe producir 8-15 shots con
intención narrativa repartidos por las secciones de la letra (hoy solo 2), cada uno con `intent` y
`shot_size` opcionales además de los campos canónicos. Y cerrar DIRQ-01 a nivel de orquestación:
fusionar `visual_bible_json` (de `visual_bible.build_visual_bible`) en el paquete que devuelven
`heuristic_onboarding_package` y `build_onboarding_package`, para que se persista y se reutilice.

Purpose: El collage genérico nace de pocos shots planos. 8-15 shots con intención por sección elevan la
densidad narrativa sin DSP. La biblia debe viajar junto al paquete para que el planner la inyecte en
cada prompt. Todo en la ruta sin key (heurística), con el LLM como mejora.
Output: `onboarding_ai.py` extendido: shots ricos por sección + clave `visual_bible_json` en el paquete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cinematic-prompting-direction-quality-f6/12-CONTEXT.md
@.planning/phases/12-cinematic-prompting-direction-quality-f6/12-PATTERNS.md
@backend/app/onboarding_ai.py
@backend/app/visual_bible.py
@backend/tests/test_onboarding_ai.py

<interfaces>
<!-- A reutilizar de onboarding_ai.py -->
def suggest_section_spans(lyrics_text) -> list[tuple[label, kind, start_line, end_line]]   # :17-44
def _keyword_attrs(brief) -> list[str]                                                      # :47-70

<!-- De visual_bible.py (plan 02) -->
def build_visual_bible(brief, lyrics_text, *, title, mood, prefer_llm) -> tuple[dict, str]

<!-- Forma de shot canónica a conservar: slug/camera/action/notes. Nuevos opcionales: intent, shot_size.
     El paquete devuelve claves *_json (str). Añadir "visual_bible_json": json.dumps(bible, ensure_ascii=False). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Shots ricos 8-15 por sección en la ruta heurística</name>
  <files>backend/app/onboarding_ai.py</files>
  <read_first>
    - backend/app/onboarding_ai.py:73-140 (heuristic_onboarding_package; hoy 2 shots :119-132)
    - backend/app/onboarding_ai.py:17-44 (suggest_section_spans: reparto por estrofa)
    - 12-CONTEXT.md §Shots ricos (DIRQ-03): 8-15 con intención, compatibilidad de campos
    - backend/tests/test_onboarding_ai.py (contrato test_shots_richness_heuristic)
  </read_first>
  <behavior>
    - heuristic_onboarding_package(...) produce un array de shots con len entre 8 y 15 (clamp por min/max).
    - El reparto usa suggest_section_spans(lyrics_text): distribuye N shots entre las secciones, dando más a
      las secciones largas; si no hay secciones (letra vacía), genera un mínimo de 8 con plantilla genérica.
    - Cada shot tiene slug, camera, action, notes (canónicos) + intent (frase de intención narrativa, p.ej.
      "presentar al sujeto", "giro emocional", "clímax") y shot_size (p.ej. "plano medio", "primer plano").
    - intent y shot_size son aditivos y opcionales; no se elimina ningún campo existente.
  </behavior>
  <action>
    En `heuristic_onboarding_package`, reemplaza la lista fija de 2 shots por un generador
    `_rich_shots(lyrics_text, brief, *, target_min=8, target_max=15) -> list[dict]` (helper puro nuevo en el
    módulo). Usa `suggest_section_spans` para obtener las secciones y reparte shots proporcionalmente al nº de
    líneas de cada sección, asignando `intent`/`shot_size` por rol de sección (apertura→presentación/plano medio,
    verso→desarrollo, estribillo→clímax/primer plano, cierre→resolución). Hereda atributos de `_keyword_attrs(brief)`
    en `notes`. Aplica clamp al rango [8,15]. Mantén `slug/camera/action/notes` en cada shot. Serializa con
    `json.dumps(..., ensure_ascii=False)` en `shots_json` (igual que hoy).
  </action>
  <verify>
    <automated>cd backend; .venv/Scripts/python.exe -m pytest tests/test_onboarding_ai.py -k shots_richness -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `json.loads(heuristic_onboarding_package(letra_varias_estrofas, brief, title=None, mood=None)["shots_json"])` tiene entre 8 y 15 elementos.
    - Cada shot tiene las claves slug, camera, action, notes, intent, shot_size.
    - Con letra vacía, devuelve al menos 8 shots (plantilla genérica), sin lanzar.
  </acceptance_criteria>
  <done>Shots ricos 8-15 con intent/shot_size en la ruta heurística; test shots_richness verde.</done>
</task>

<task type="auto">
  <name>Tarea 2: Fusionar visual_bible_json en el paquete (heurística + LLM)</name>
  <files>backend/app/onboarding_ai.py</files>
  <read_first>
    - backend/app/onboarding_ai.py:133-140 (claves del paquete heurístico) y :235-242 (paquete LLM)
    - backend/app/onboarding_ai.py:247-269 (build_onboarding_package: degradación)
    - backend/app/visual_bible.py (build_visual_bible — plan 02)
    - 12-CONTEXT.md §Biblia visual (persistida y reutilizada en TODOS los segmentos)
  </read_first>
  <action>
    Importa `from .visual_bible import build_visual_bible` (sin imports pesados nuevos). En
    `heuristic_onboarding_package` añade al dict devuelto la clave
    `"visual_bible_json": json.dumps(build_visual_bible(brief, lyrics_text, title=title, mood=mood, prefer_llm=False)[0], ensure_ascii=False)`.
    En `build_onboarding_package`, tras obtener el paquete (LLM o heurístico), garantiza que `visual_bible_json`
    esté presente: si la ruta LLM (`llm_onboarding_package`) no lo incluye, genera la biblia con
    `build_visual_bible(..., prefer_llm=prefer_llm)` y añádela al paquete antes de devolver. Mantén el patrón de
    degradación intacto (nunca lanzar; devolver (paquete, nota)). En la ruta LLM puedes opcionalmente pedir la
    biblia en el mismo JSON (clave "visual_bible") y mapearla — pero el contrato mínimo es que el paquete final
    SIEMPRE traiga `visual_bible_json` válido.
    NOTA: el plan 06 leerá `visual_bible_json` del paquete/Song para inyectarla en los prompts.
  </action>
  <verify>
    <automated>cd backend; .venv/Scripts/python.exe -m pytest tests/test_onboarding_ai.py -x -q; cd ..; cd backend; .venv/Scripts/python.exe -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `build_onboarding_package(letra, brief, title=None, mood=None, language="es", prefer_llm=False)[0]` contiene la clave `visual_bible_json`.
    - `json.loads(...["visual_bible_json"])` tiene las 8 claves de la biblia (subject/world/palette/optics/light_rule/grain_dof/style_attributes/negatives).
    - Suite completa verde (≥48 + nuevos); pipeline.py sigue cargando `shots_json` sin error.
  </acceptance_criteria>
  <done>visual_bible_json presente en el paquete por ambas rutas; suite verde.</done>
</task>

</tasks>

<verification>
- `cd backend && .venv/Scripts/python.exe -m pytest tests/test_onboarding_ai.py -x -q` verde.
- `cd backend && .venv/Scripts/python.exe -m pytest -q` verde (≥48 + nuevos); test_smoke (que ejercita onboarding vía API) sin romper.
</verification>

<success_criteria>
DIRQ-03 cumplido (8-15 shots ricos sin key) y DIRQ-01 orquestado (biblia fusionada y persistible en el
paquete). El planner (plan 06) puede leer shots ricos + biblia para construir los prompts por capas.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → shots/biblia | salida no confiable (IP); ya saneada por visual_bible en la biblia |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Information disclosure / IP | LLM cuela nombre de obra/artista en shots/biblia | mitigate | biblia pasa por sanitize_visual_bible (plan 02); system-prompt de shots ya prohíbe nombres reales (onboarding_ai.py:172) |
| T-12-09 | DoS | JSON LLM malformado | mitigate | parseo defensivo + fallback heurístico ya existente (build_onboarding_package) |
</threat_model>

<output>
Tras completar, crea `.planning/phases/12-cinematic-prompting-direction-quality-f6/12-05-SUMMARY.md`
</output>
