---
phase: 12-cinematic-prompting-direction-quality-f6
plan: 03
type: execute
wave: 1
depends_on: [12-01]
files_modified:
  - backend/app/camera_language.py
autonomous: true
requirements: [DIRQ-04]

must_haves:
  truths:
    - "Una función pura mapea el nivel de energía a vocabulario de cámara (shot_size + camera_move)"
    - "Alta energía produce planos más cerrados/dinámicos; baja energía planos sostenidos"
    - "Sin audio (solo-letra) el mapeo cae a un perfil neutro y no rompe"
  artifacts:
    - path: "backend/app/camera_language.py"
      provides: "camera_for_energy(level) + energy_level_for(...) puros, sin I/O"
      contains: "def camera_for_energy"
  key_links:
    - from: "backend/app/camera_language.py"
      to: "music_planner._materialize (plan 06)"
      via: "camera_for_energy consumido por segmento según energía de sección"
      pattern: "camera_for_energy"
---

<objective>
Implementar DIRQ-04 (pieza pura): un módulo `camera_language.py` con el mapa energía→vocabulario de
cámara. Es un helper puro y aislado (sin dependencias del planner) para poder testearlo sin audio. El
plan 06 lo consume dentro de `music_planner._materialize`.

Purpose: La modulación de plano/cámara por energía es lo que hace que el estribillo "sienta" distinto
del puente. Aislar el vocabulario en una función pura lo hace testeable y ajustable (vocabulario
concreto = "Claude's Discretion" en CONTEXT.md) sin tocar el planner.
Output: módulo `camera_language.py` con `camera_for_energy(level)` y un clasificador `energy_level_for`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cinematic-prompting-direction-quality-f6/12-CONTEXT.md
@.planning/phases/12-cinematic-prompting-direction-quality-f6/12-PATTERNS.md
@backend/app/music_planner.py
@backend/tests/test_camera_language.py

<interfaces>
<!-- Helpers de energía del planner (music_planner.py:56-70), referencia conceptual:
     _section_energy_threshold(sections) -> mediana de mean_energy
     _is_high_energy(t, sections, threshold) -> bool  (False si no hay secciones → degradación) -->
<!-- Pattern 3 del research (vocabulario objetivo):
     high  → shot_size "primer plano / plano medio corto", camera_move "push-in rápido, handheld, whip-pan"
     mid   → "plano medio", "dolly suave, tracking lateral"
     low   → "plano general sostenido", "cámara estática o pan muy lento"
     neutral (sin audio) → "plano medio", "movimiento contenido" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: camera_language.py — mapa energía→cámara puro</name>
  <files>backend/app/camera_language.py</files>
  <read_first>
    - 12-RESEARCH.md §Pattern 3 (ENERGY_CAMERA, camera_for_energy)
    - 12-PATTERNS.md §music_planner.py (helper puro recomendado, vocabulario por nivel)
    - backend/app/music_planner.py:56-70 (_section_energy_threshold / _is_high_energy, para alinear semántica)
    - backend/tests/test_camera_language.py (contrato a satisfacer)
  </read_first>
  <behavior>
    - camera_for_energy("high"|"mid"|"low"|"neutral") → dict {"shot_size": str, "camera_move": str}.
    - Un nivel desconocido o None cae a "neutral".
    - high.shot_size es más cerrado que low.shot_size; high.camera_move más dinámico que low (verificable: difieren).
    - energy_level_for(mean_energy, threshold) → "high" si mean_energy>threshold; "low" si por debajo de
      threshold; "neutral" si threshold es None (sin audio). (El umbral lo calcula el planner; aquí solo clasifica.)
  </behavior>
  <action>
    Crea `backend/app/camera_language.py`. Solo stdlib (`from __future__ import annotations`, `typing`).
    Define `ENERGY_CAMERA: dict[str, dict[str, str]]` con las 4 entradas high/mid/low/neutral siguiendo el
    vocabulario del §Pattern 3 (ajustable; es discreción del autor). Implementa
    `camera_for_energy(level: str) -> dict[str, str]` con fallback a "neutral", y
    `energy_level_for(mean_energy: float | None, threshold: float | None) -> str` que devuelve "neutral"
    cuando threshold es None (degradación sin audio), si no compara contra el umbral (>umbral → "high",
    en torno/por debajo → "mid"/"low"; define un margen simple, p.ej. mid si está dentro de ±10% del umbral).
    Para Kling, incluye en `camera_move` un "endpoint" de movimiento (p.ej. "…y se asienta") en al menos
    el nivel high, para evitar el anti-patrón "camera move sin endpoint" del research (§Anti-Patterns).
    NADA de I/O ni imports del planner (debe ser importable aislado).
  </action>
  <verify>
    <automated>cd backend; .venv/Scripts/python.exe -m pytest tests/test_camera_language.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `camera_for_energy("high")` y `camera_for_energy("low")` devuelven dicts con claves shot_size y camera_move que DIFIEREN entre sí.
    - `camera_for_energy("desconocido") == camera_for_energy("neutral")`.
    - `energy_level_for(0.9, None) == "neutral"` (sin audio); `energy_level_for(0.9, 0.5) == "high"`.
    - `import app.camera_language` no importa music_planner ni nada pesado.
  </acceptance_criteria>
  <done>camera_language.py implementado; test_camera_language verde; helper puro aislado.</done>
</task>

</tasks>

<verification>
- `cd backend && .venv/Scripts/python.exe -m pytest tests/test_camera_language.py -x -q` verde.
- `cd backend && .venv/Scripts/python.exe -m pytest -q` verde (≥48 + nuevos).
</verification>

<success_criteria>
DIRQ-04 (pieza pura) lista: vocabulario de cámara modulado por energía, con degradación neutral sin audio,
testeado en aislamiento. El plan 06 lo cableará al planner.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguno) | Función pura de strings, sin entrada externa no confiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | (n/a) | camera_language puro | accept | Sin superficie de ataque; solo datos estáticos |
</threat_model>

<output>
Tras completar, crea `.planning/phases/12-cinematic-prompting-direction-quality-f6/12-03-SUMMARY.md`
</output>
