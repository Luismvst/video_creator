---
phase: 12-cinematic-prompting-direction-quality-f6
plan: 07
type: execute
wave: 4
depends_on: [12-01, 12-06]
files_modified:
  - backend/app/keyframes.py
  - backend/app/render_client.py
autonomous: true
requirements: [DIRQ-05, DIRQ-06, DIRQ-07]

must_haves:
  truths:
    - "El plan de keyframes encadena: el segmento i referencia el keyframe del i-1; el primero no encadena"
    - "El camino real pasa el frame anterior como init-image (image_url) al image-to-video del segmento i"
    - "El plan de init-image es verificable en dry-run sin gastar"
    - "render_timeline(reroll_indices=[...]) regenera SOLO los segmentos marcados; estimación sobre el subconjunto"
    - "Sin reroll_indices el comportamiento previo se mantiene (no regresión)"
    - "veo3/veo3_fast enrutan a prompt_veo (con fallback a prompt_generic si prompt_veo está vacío): Veo cableado de extremo a extremo"
  artifacts:
    - path: "backend/app/keyframes.py"
      provides: "camino real i2v con image_url derivado de reference_image; helper de payload verificable"
      contains: "image_url"
    - path: "backend/app/render_client.py"
      provides: "render_timeline(reroll_indices=...) + image_url en render_segment (i2v) + PROVIDERS veo3/veo3_fast → prompt_veo"
      contains: "reroll_indices"
  key_links:
    - from: "backend/app/keyframes.py"
      to: "render_client._http_json"
      via: "reutilizar gateway fal (misma auth) para el camino real"
      pattern: "_http_json|queue.fal.run"
    - from: "backend/app/render_client.py"
      to: "render_segment(image_url=...)"
      via: "i2v: pasar init-image al payload fal"
      pattern: "image_url"
    - from: "backend/app/render_client.py"
      to: "provider_prompt (veo3/veo3_fast)"
      via: "PROVIDERS[*].prompt_field = prompt_veo con fallback a prompt_generic"
      pattern: "prompt_veo"
---

<objective>
Cerrar DIRQ-05 (keyframe encadenado real / init-image), DIRQ-07 (reroll dirigido) y completar DIRQ-06 para
Veo (enrutado de `prompt_veo`). El plan de keyframes ya encadena `reference_image` (verificable en dry-run);
aquí (a) se hace que el plan exponga explícitamente la init-image por segmento y (b) el camino real (aislado
tras `dry_run=False`+`FAL_KEY`) pasa ese frame como `image_url` al modelo image-to-video. `render_timeline`
gana `reroll_indices` para regenerar solo los segmentos marcados. Y `PROVIDERS["veo3"]`/`PROVIDERS["veo3_fast"]`
pasan a `prompt_field="prompt_veo"` para que el texto cámara-primero (plan 04/06) llegue realmente al motor
Veo — cerrando el cableado de Veo de extremo a extremo (Q3 RESUELTO, H2).

Purpose: La continuidad anti-collage real depende del last-frame chaining. El reroll dirigido evita
re-renderizar (y re-pagar) toda la canción para arreglar 2 clips flojos. Y hasta hoy el tuning Veo se
compilaba pero `PROVIDERS` mapeaba veo3/veo3_fast → prompt_generic, así que nunca llegaba a un proveedor;
este plan lo enruta a prompt_veo (con fallback seguro). Todo verificable en dry-run.
Output: `keyframes.py` con camino real i2v + helper de payload; `render_client.py` con reroll por índices,
`image_url` en el render por segmento, y veo3/veo3_fast enrutados a prompt_veo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cinematic-prompting-direction-quality-f6/12-CONTEXT.md
@.planning/phases/12-cinematic-prompting-direction-quality-f6/12-PATTERNS.md
@backend/app/keyframes.py
@backend/app/render_client.py
@backend/tests/test_keyframes.py
@backend/tests/test_render_client.py

<interfaces>
<!-- keyframes.plan_keyframes:53-93 ya produce items con reference_image=prev_path (encadenado). -->
<!-- render_client.PROVIDERS:43-69 — veo3/veo3_fast hoy con prompt_field="prompt_generic" (a cambiar). -->
<!-- render_client.provider_prompt:82-85 — ya hace fallback: segment.get(field) or segment.get("prompt_generic").
     Por tanto basta con cambiar prompt_field a "prompt_veo"; el fallback a prompt_generic es automático si vacío. -->
<!-- render_client._http_json:243-251 (auth "Key {key}") y _fal_generate:254-304 (queue API). -->
<!-- render_segment:143-183 (dry-run + camino real); render_timeline:186-236 (limit ya existe). -->
<!-- Naming estable: clip_{idx:03d}.mp4 / kf_{idx:03d}.png — NO cambiar (video_assembly los resuelve). -->
<!-- PROVIDERS[*].fal_model apunta a text-to-video; para i2v añadir fal_model_i2v configurable por env. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Reroll dirigido + image_url (i2v) en render_client + enrutado prompt_veo para veo3/veo3_fast</name>
  <files>backend/app/render_client.py</files>
  <read_first>
    - backend/app/render_client.py:43-69 (PROVIDERS: veo3/veo3_fast con prompt_field="prompt_generic") y :82-85 (provider_prompt con fallback)
    - backend/app/render_client.py:186-236 (render_timeline con limit) y :143-183 (render_segment)
    - backend/app/render_client.py:254-304 (_fal_generate: payload fal)
    - 12-RESEARCH.md §Pattern 5 (reroll por índices) + §Pattern 4 (init-image)
    - 12-RESEARCH.md §Open Questions Q3 (RESUELTO: PROVIDERS veo3/veo3_fast → prompt_veo, fallback prompt_generic)
    - 12-PATTERNS.md §render_client.py (parámetro opcional, naming estable, estimación sobre subconjunto)
    - backend/tests/test_render_client.py (test_reroll_only_marked_indices, image_url en payload, provider_prompt veo)
  </read_first>
  <behavior>
    - PROVIDERS["veo3"]["prompt_field"] == "prompt_veo" y PROVIDERS["veo3_fast"]["prompt_field"] == "prompt_veo".
      provider_prompt(seg, "veo3") devuelve seg["prompt_veo"] cuando existe y no está vacío; si prompt_veo está
      vacío/ausente, cae automáticamente a seg["prompt_generic"] (fallback ya presente en provider_prompt). Los
      demás proveedores (kling/runway/wan) NO cambian su prompt_field.
    - render_timeline(..., reroll_indices: Optional[list[int]] = None): si se da, to_render = segmentos cuyo
      `index` está en set(reroll_indices), preservando orden; la estimación se calcula sobre ESE subconjunto;
      el gate de presupuesto aplica sobre el subconjunto. Sin reroll_indices, comportamiento idéntico al actual
      (compatible con `limit`).
    - render_segment(..., image_url: Optional[str] = None): en dry-run, el dict de salida incluye
      `image_url` (o None) para que el plan i2v sea verificable; en camino real, el payload fal incluye `image_url`
      cuando se proporciona (i2v) y usa `fal_model_i2v` si está configurado, si no el fal_model normal.
    - Naming de clip estable (clip_{idx:03d}.mp4) para que el reroll sobrescriba el clip correcto.
  </behavior>
  <action>
    En `render_client.py`:
    (1) En `PROVIDERS`, cambia `prompt_field` de `"veo3"` y `"veo3_fast"` de `"prompt_generic"` a `"prompt_veo"`.
    No toques `provider_prompt` (su fallback `segment.get(field) or segment.get("prompt_generic")` ya cubre el
    caso de prompt_veo vacío). No cambies kling/runway/wan.
    (2) Añade `reroll_indices: Optional[list[int]] = None` a `render_timeline`; calcula `to_render` con prioridad
    reroll_indices > limit > todos (un único punto de selección declarativo).
    (3) Añade `image_url: Optional[str] = None` a `render_segment`; en la rama dry-run incluye `"image_url":
    image_url` en el dict; en la rama real, pásalo a `_fal_generate`. Extiende `_fal_generate` para aceptar
    `image_url: Optional[str] = None` y, si no es None, añadir `payload["image_url"] = image_url` (i2v). Añade un
    `fal_model_i2v` configurable por env en cada PROVIDERS[*] (o un override global `VIDEOZERO_FAL_MODEL_I2V`):
    cuando hay image_url, usa ese modelo si está definido. La estimación de coste y `within_budget` ya operan sobre
    `to_render`; no dupliques lógica. No cambies firmas existentes salvo añadir kwargs opcionales.
  </action>
  <verify>
    <automated>cd backend; .venv/Scripts/python.exe -m pytest tests/test_render_client.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `provider_prompt({"prompt_veo": "VEO...", "prompt_generic": "GEN..."}, "veo3")` devuelve el prompt Veo ("VEO...").
    - `provider_prompt({"prompt_generic": "GEN..."}, "veo3")` (sin prompt_veo) cae a "GEN..." (fallback intacto).
    - PROVIDERS["veo3"]["prompt_field"] == PROVIDERS["veo3_fast"]["prompt_field"] == "prompt_veo"; kling/runway/wan sin cambios.
    - `render_timeline(_segs(10), provider="kling", dry_run=True, reroll_indices=[2,5])` → los index renderizados son exactamente [2,5] y estimate.n_segments == 2.
    - `render_segment(seg, dry_run=True, image_url="kf_001.png")["image_url"] == "kf_001.png"`.
    - Sin reroll_indices, todos los tests existentes de render_client siguen verdes.
  </acceptance_criteria>
  <done>veo3/veo3_fast → prompt_veo (fallback intacto); reroll por índices + image_url en dry-run; estimación sobre subconjunto; tests verdes; sin regresión.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: Camino real i2v en keyframes (init-image = frame anterior)</name>
  <files>backend/app/keyframes.py</files>
  <read_first>
    - backend/app/keyframes.py:33-93 (keyframe_prompt, plan_keyframes chain)
    - backend/app/render_client.py:243-304 (_http_json + _fal_generate: molde del camino real fal)
    - 12-RESEARCH.md §Pattern 4 + §Assumptions A1/Q2 (esquema fal i2v configurable por env)
    - backend/tests/test_keyframes.py (test del helper build_keyframe_payload + image_url; el encadenado base vive en test_pipeline.py)
  </read_first>
  <behavior>
    - El plan dry-run sigue dando reference_image encadenado (ya probado en test_pipeline.py) — NO romper.
    - Existe un helper puro que construye el payload de generación de keyframe i incluyendo
      `image_url = reference_image` (el kf del i-1) cuando hay encadenado; el primero sin image_url.
    - El camino real (dry_run=False + FAL_KEY) envía a fal vía queue API (mismo molde que _fal_generate),
      incluyendo image_url para el i2v/edición encadenada; aislado, no se ejercita en CI.
  </behavior>
  <action>
    En `keyframes.py` añade un helper puro `build_keyframe_payload(item: dict, prompt: str) -> dict` que devuelva
    `{"prompt": prompt, ...}` y, si `item.get("reference_image")`, añada `"image_url": item["reference_image"]`.
    Úsalo tanto en el plan (para exponer `image_url` en cada item del plan, verificable en dry-run) como en el
    camino real. Implementa el camino real `generate_keyframes(plan_or_segments, ..., dry_run=False, fal_key=...)`
    (o extiende `plan_keyframes` con una rama real tras dry_run=False) copiando el molde de
    `render_client._fal_generate` (queue.fal.run, polling, urlretrieve). Reutiliza `render_client._http_json`
    (impórtalo) para no duplicar la llamada/auth. Mantén los imports pesados (urllib) DENTRO del camino real o a
    nivel módulo solo stdlib — no añadas nada que CI no tenga. Modelo configurable por env
    (`VIDEOZERO_FAL_MODEL_IMAGE` ya existe; añade `VIDEOZERO_FAL_MODEL_IMAGE_I2V` si el i2v usa otro endpoint).
    El plan dry-run debe seguir verde sin tocar su contrato (reference_image encadenado).
  </action>
  <verify>
    <automated>cd backend; .venv/Scripts/python.exe -m pytest tests/test_keyframes.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - El encadenado base (test_pipeline.py::test_plan_keyframes_chains_references) sigue verde (plan encadenado intacto).
    - `build_keyframe_payload({"reference_image":"kf_001.png"}, "p")["image_url"] == "kf_001.png"` y para `{"reference_image": None}` NO incluye image_url.
    - Cada item del plan dry-run expone `image_url` (None para el primero, kf anterior para el resto) — DIRQ-05 verificable sin gastar.
    - `import app.keyframes` no carga nada pesado a nivel módulo.
  </acceptance_criteria>
  <done>init-image real cableada vía payload; plan verificable en dry-run; encadenado intacto; tests verdes.</done>
</task>

</tasks>

<verification>
- `cd backend && .venv/Scripts/python.exe -m pytest tests/test_render_client.py tests/test_keyframes.py -x -q` verde.
- `cd backend && .venv/Scripts/python.exe -m pytest -q` verde (≥48 + nuevos) — gate de fin de fase.
</verification>

<success_criteria>
DIRQ-05 (keyframe encadenado real con init-image, plan verificable en dry-run), DIRQ-07 (reroll dirigido
por índices con estimación sobre el subconjunto) y DIRQ-06 para Veo (veo3/veo3_fast enrutan a prompt_veo con
fallback a prompt_generic) cumplidos sin regresión. Con esto Veo queda cableado de extremo a extremo y los
7 DIRQ quedan cubiertos para los 3 motores.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FAL_KEY (env) → camino real | secreto; solo en headers, nunca en plan/segments.json |
| respuesta fal → descarga MP4/PNG | I/O de red en camino real aislado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-12 | Information disclosure | FAL_KEY filtrada en plan o salida | mitigate | key solo en header `Key {key}`; nunca en items del plan ni en dry-run output |
| T-12-13 | Tampering | romper naming de clip → assembly no encuentra el reroll | mitigate | conservar clip_{idx:03d}.mp4 / kf_{idx:03d}.png; reroll sobrescribe por índice |
| T-12-14 | DoS | esquema fal i2v cambia (image_url) | accept | camino real aislado tras dry_run=False+FAL_KEY; modelo/campos configurables por env (A1/Q2) |
| T-12-15 | Tampering | veo3 enruta a prompt_veo vacío → prompt en blanco al motor | mitigate | provider_prompt mantiene fallback a prompt_generic si prompt_veo está vacío; test lo verifica |
</threat_model>

<output>
Tras completar, crea `.planning/phases/12-cinematic-prompting-direction-quality-f6/12-07-SUMMARY.md`
</output>
</content>
