---
phase: 12-cinematic-prompting-direction-quality-f6
plan: 02
type: execute
wave: 1
depends_on: [12-01]
files_modified:
  - backend/app/visual_bible.py
  - backend/app/models.py
  - backend/app/schemas.py
  - backend/app/json_fields.py
autonomous: true
requirements: [DIRQ-01]

must_haves:
  truths:
    - "Existe una biblia visual estructurada con los 8 campos generada SIN API key (heurística)"
    - "El LLM solo mejora; el camino sin key nunca falla ni lanza"
    - "Un sanitizador legal elimina nombres de obra/artista de la biblia antes de devolverla"
    - "La biblia se persiste en un campo JSON reutilizable por todos los segmentos"
  artifacts:
    - path: "backend/app/visual_bible.py"
      provides: "heuristic_visual_bible + llm_visual_bible + build_visual_bible + sanitize_visual_bible"
      contains: "def build_visual_bible"
    - path: "backend/app/models.py"
      provides: "Song.visual_bible_json (columna JSON opcional, aditiva, sin migración)"
      contains: "visual_bible_json"
  key_links:
    - from: "backend/app/visual_bible.py"
      to: "lyrics_insights_engine"
      via: "import config OpenAI (no duplicar)"
      pattern: "_openai_api_key|openai_configured|_openai_model"
    - from: "backend/app/visual_bible.py"
      to: "sanitize_visual_bible"
      via: "build_visual_bible aplica el sanitizador post-LLM"
      pattern: "sanitize_visual_bible"
---

<objective>
Implementar DIRQ-01: una biblia visual estructurada con campos fijos, generada por heurística sin key
y mejorada por LLM cuando hay key, con un sanitizador legal que impide colar nombres de obra/artista.
Resolver la pregunta abierta de persistencia: añadir `visual_bible_json` como campo JSON opcional en
`Song` (aditivo, sin tooling de migración — patrón idéntico a los JSON-blobs existentes).

Purpose: La biblia es el ancla anti-collage. Debe ser ÚNICA por canción y reutilizable en todos los
segmentos (planes 04/06 la inyectan). Es contenido de texto derivado del brief/letra → 100% testeable
con asserts puros, sin red.
Output: módulo `visual_bible.py` (patrón triádico heuristic_/llm_/build_) + campo persistido + sanitizador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cinematic-prompting-direction-quality-f6/12-CONTEXT.md
@.planning/phases/12-cinematic-prompting-direction-quality-f6/12-PATTERNS.md
@backend/app/onboarding_ai.py
@backend/tests/test_visual_bible.py

<interfaces>
<!-- Config OpenAI a IMPORTAR (no duplicar), de lyrics_insights_engine.py:121-130 -->
def _openai_api_key() -> Optional[str]   # VIDEOZERO_OPENAI_API_KEY | OPENAI_API_KEY
def openai_configured() -> bool
def _openai_model() -> str               # default "gpt-4o-mini"

<!-- Pool heurístico reutilizable de onboarding_ai.py:47-70 -->
def _keyword_attrs(brief: str) -> list[str]   # 3-5 atributos, nunca nombres de obra/artista

<!-- Campo persistido: Song es SQLModel (models.py:19). Las columnas JSON-blob existentes
     son Optional[str]=None y se añaden SIN migración (SQLite dev). json_fields._JSON_PATCH_KEYS
     lista las claves que el API valida como JSON. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Módulo visual_bible.py (heurística + LLM + sanitizador)</name>
  <files>backend/app/visual_bible.py</files>
  <read_first>
    - backend/app/onboarding_ai.py:73-140 (heuristic_onboarding_package: molde a clonar) y :143-244 (llm_ via urllib)
    - backend/app/lyrics_insights_engine.py:121-130 (config OpenAI a importar)
    - 12-PATTERNS.md §visual_bible.py (firmas recomendadas, patrón triádico)
    - 12-RESEARCH.md §Pattern 2 + §Pitfall 4 (sanitizador legal)
    - backend/tests/test_visual_bible.py (contrato a satisfacer)
  </read_first>
  <behavior>
    - heuristic_visual_bible(brief, lyrics_text, title=None, mood=None) → dict con las 8 claves:
      subject, world, palette, optics, light_rule, grain_dof, style_attributes (list), negatives (list).
    - style_attributes deriva de `_keyword_attrs` (importado de onboarding_ai); nunca nombres de obra/artista.
    - negatives = constante DEFAULT_NEGATIVES (p.ej. "texto en pantalla", "manos deformes", "marca de agua").
    - sanitize_visual_bible(bible) elimina de style_attributes (y de cualquier campo string) entradas que
      contengan los marcadores prohibidos: "estilo de ", "como ", "tipo ", o un nombre propio capitalizado
      precedido de esos marcadores. Devuelve la biblia limpia (no muta la original).
    - build_visual_bible(..., prefer_llm) → tupla (bible_dict, note): si prefer_llm y openai_configured()
      intenta llm_visual_bible; SIEMPRE aplica sanitize_visual_bible al resultado; cae a heurística si el LLM
      falla o no hay key; nunca lanza.
    - llm_visual_bible(...) → (Optional[dict], Optional[str]): urllib a OpenAI, response_format json_object,
      system-prompt que prohíbe explícitamente nombres de obra/artista; parseo defensivo con limpieza de fences.
  </behavior>
  <action>
    Crea `backend/app/visual_bible.py`. Imports a nivel módulo SOLO stdlib (`json`, `os`, `re`,
    `urllib.request`, `urllib.error`, `typing`) + `from .lyrics_insights_engine import _openai_api_key,
    _openai_model, openai_configured` + `from .onboarding_ai import _keyword_attrs`. NADA pesado.
    Define la constante `DEFAULT_NEGATIVES: list[str]` y la lista `_FORBIDDEN_MARKERS = ["estilo de ", "al estilo de ", "como el de ", "tipo "]`.
    Implementa `heuristic_visual_bible`, `sanitize_visual_bible`, `llm_visual_bible`, `build_visual_bible`
    siguiendo el patrón triádico de `onboarding_ai.build_onboarding_package:247-269` (degradación elegante,
    devolver (resultado, nota), nunca raise). El system-prompt del LLM debe incluir literalmente la regla
    "referencias→atributos, nunca nombres de obras o artistas reales" (igual que onboarding_ai.py:172).
    Aplica `sanitize_visual_bible` SIEMPRE en build_visual_bible, también tras la heurística (defensa en
    profundidad). No imports pesados, sin red en el camino heurístico.
  </action>
  <verify>
    <automated>cd backend; .venv/Scripts/python.exe -m pytest tests/test_visual_bible.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `build_visual_bible("puerto nevado al amanecer", "letra...", prefer_llm=False)` devuelve (dict, str) con las 8 claves; style_attributes y negatives son listas no vacías.
    - `sanitize_visual_bible({"style_attributes":["estilo de Tarantino","luz neón"]})["style_attributes"] == ["luz neón"]`.
    - Importar el módulo NO ejecuta ninguna llamada de red y NO requiere ninguna env var.
    - test_visual_bible.py pasa (los skips del plan 01 se vuelven verdes).
  </acceptance_criteria>
  <done>visual_bible.py implementado; test_visual_bible verde; sin imports pesados; degradación sin key probada.</done>
</task>

<task type="auto">
  <name>Tarea 2: Persistir visual_bible_json en Song (aditivo, sin migración)</name>
  <files>backend/app/models.py, backend/app/schemas.py, backend/app/json_fields.py</files>
  <read_first>
    - backend/app/models.py:19-63 (Song: columnas JSON Optional[str]=None)
    - backend/app/schemas.py:23-42 (SongUpdate) y :190-220 (SongRead)
    - backend/app/json_fields.py:6-19 (_JSON_PATCH_KEYS)
  </read_first>
  <action>
    En `models.py` añade a `Song` el campo `visual_bible_json: Optional[str] = None` junto a los demás
    JSON-blobs (tras `shots_json`), con comentario "# DIRQ-01: biblia visual estructurada, reutilizada en todos los segmentos".
    En `schemas.py` añade `visual_bible_json: Optional[str] = None` a `SongUpdate` y a `SongRead`
    (misma posición relativa que `shots_json`).
    En `json_fields.py` añade `"visual_bible_json"` al frozenset `_JSON_PATCH_KEYS` para que el API lo valide
    como JSON string.
    NO crear migración Alembic (no existe tooling; SQLite dev recrea el esquema). Documenta en el SUMMARY que en
    dev puede requerir borrar `backend/data/videozero.db` (concern ya conocido en STATE.md).
  </action>
  <verify>
    <automated>cd backend; .venv/Scripts/python.exe -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `Song` tiene atributo `visual_bible_json`; `SongUpdate` y `SongRead` lo aceptan/exponen.
    - `visual_bible_json` está en `_JSON_PATCH_KEYS`.
    - Suite completa verde (≥48 + nuevos); ningún test existente roto.
  </acceptance_criteria>
  <done>Campo persistible añadido en model+schemas+json_fields; suite verde; sin migración.</done>
</task>

</tasks>

<verification>
- `cd backend && .venv/Scripts/python.exe -m pytest tests/test_visual_bible.py -x -q` verde.
- `cd backend && .venv/Scripts/python.exe -m pytest -q` verde (≥48 + nuevos).
- `import app.visual_bible` no dispara red ni exige env vars.
</verification>

<success_criteria>
DIRQ-01 cumplido: biblia con 8 campos estructurados, heurística sin key, LLM opcional, sanitizador legal
verificado por test, y campo persistido reutilizable. Listo para que los planes 04/06 inyecten la biblia.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM (OpenAI) → biblia/prompt | Salida no confiable: puede colar nombres de obra/artista (riesgo IP) |
| API key (env) | Secreto: nunca en biblia, logs ni segments.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Information disclosure / IP | LLM inyecta nombre de obra/artista en style_attributes | mitigate | system-prompt restrictivo + `sanitize_visual_bible` post-LLM + test que lo verifica |
| T-12-03 | DoS | JSON malformado del LLM rompe el flujo | mitigate | try/except + fallback heurístico (patrón onboarding_ai) |
| T-12-04 | Information disclosure | API key filtrada en la biblia persistida | accept | key solo por env; build_visual_bible nunca escribe la key en el dict |
</threat_model>

<output>
Tras completar, crea `.planning/phases/12-cinematic-prompting-direction-quality-f6/12-02-SUMMARY.md`
</output>
